<!doctype html>
<title>Clue-Less</title>

<h1>Your Guess was {{ suspect }} in the {{ room }} with the {{ weapon }}...</h1>
(sent by: {{player}})
<h1>Your Guess was
  {% if valid %}
  Correct! Wonderful Job!
  {% else %}
  Incorrect!
  {% endif %}
</h1>

{# <form method="post">
  <input type="submit" value="Guess Again">
  <input type="hidden" id="player" name="player" value={{player}}>
</form> #}
